# Lebanon Prices Intelligence Unit — environment variables
# Copy this file to .env.local and fill in your key, then restart `npm run dev`.

# Required for the AI Price Economist (the right-side chat dock).
# Get one at https://console.anthropic.com/  (starts with "sk-ant-").
ANTHROPIC_API_KEY=
